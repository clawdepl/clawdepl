---
title: clawdepl logout
description: Shortcut alias for `clawdepl auth logout`.
---

`clawdepl logout` is an alias for [`clawdepl auth logout`](/cli/auth#auth-logout).

```bash
clawdepl logout
```

Clears stored credentials from `~/.clawdepl/credentials.json`.
